import sys, re, collections
from os import listdir
from os.path import isfile, join
import numpy as np
from operator import itemgetter

p = collections.defaultdict(lambda:np.zeros(3))
print p[1][2]
print len(p[2]);
